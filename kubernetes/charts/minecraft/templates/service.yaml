apiVersion: v1
kind: Service
metadata:
  name: minecraft
spec:
  type: ClusterIP
  ports:
    - port: 25565
      name: minecraft
      protocol: TCP
    - port: 25575
      name: rcon
      protocol: TCP
  selector:
    app: minecraft