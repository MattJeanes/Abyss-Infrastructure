apiVersion: apps/v1
kind: Deployment
metadata:
  name: minecraftmap-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minecraftmap-proxy
  template:
    metadata:
      labels:
        app: minecraftmap-proxy
    spec:
      containers:
      - name: caddy
        image: caddy:latest
        volumeMounts:
        - name: caddy-config
          mountPath: /etc/caddy/Caddyfile
          subPath: Caddyfile
      volumes:
      - name: caddy-config
        configMap:
          name: minecraftmap-proxy-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: minecraftmap-proxy-config
data:
  Caddyfile: |
    :80 {
      reverse_proxy 192.168.68.126:8123 {
        trusted_proxies private_ranges
      }
    }
---
apiVersion: v1
kind: Service
metadata:
  name: minecraftmap-proxy
spec:
  selector:
    app: minecraftmap-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minecraftmap-migration
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - map.minecraft.mattjeanes.com
      secretName: minecraftmap-tls
  rules:
  - host: map.minecraft.mattjeanes.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: minecraftmap-proxy
            port:
              number: 80
