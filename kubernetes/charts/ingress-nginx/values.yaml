ingress-nginx:
  controller:
    resources:
      requests:
        cpu: 100m
        memory: 250Mi
    service:
      externalTrafficPolicy: Local
    config:
      annotations-risk-level: Critical
    allowSnippetAnnotations: true