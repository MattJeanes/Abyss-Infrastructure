apiVersion: apps/v1
kind: Deployment
metadata:
  name: plex-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plex-proxy
  template:
    metadata:
      labels:
        app: plex-proxy
    spec:
      containers:
      - name: caddy
        image: caddy:latest
        volumeMounts:
        - name: caddy-config
          mountPath: /etc/caddy/Caddyfile
          subPath: Caddyfile
      volumes:
      - name: caddy-config
        configMap:
          name: plex-proxy-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: plex-proxy-config
data:
  Caddyfile: |
    :80 {
      reverse_proxy 192.168.68.126:32400 {
        trusted_proxies private_ranges
      }
    }
---
apiVersion: v1
kind: Service
metadata:
  name: plex-proxy
spec:
  selector:
    app: plex-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex-migration
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - plex.mattjeanes.com
      secretName: plex-tls
  rules:
  - host: plex.mattjeanes.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: plex-proxy
            port:
              number: 80
