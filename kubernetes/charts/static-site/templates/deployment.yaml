apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
      - name: app
        image: httpd
        ports:
        - containerPort: 80
          protocol: TCP
        imagePullPolicy: Always
        resources: {{ toYaml .Values.resources | nindent 10 }}
        volumeMounts:
        - name: site
          mountPath: /usr/local/apache2/htdocs
          {{- if .Values.mount.subPath }}
          subPath: {{ .Values.mount.subPath }}
          {{- end }}
        livenessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 4
          failureThreshold: 5
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 4
          periodSeconds: 5
          timeoutSeconds: 2
          successThreshold: 1
          failureThreshold: 2
      volumes:
      - name: site
        csi:
          driver: file.csi.azure.com
          volumeAttributes:
            secretName: {{ .Values.mount.secretName }}
            shareName: {{ required "mount.shareName is required" .Values.mount.shareName }}